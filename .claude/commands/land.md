# /vibenotch:land — Commit et finalize

## Pré-requis (Quality Gate)
```bash
swift build    # DOIT passer
swift test     # DOIT passer
```

## Workflow
1. `git status` — vérifier les changements
2. `git diff` — relire les modifications
3. Commit avec message descriptif (anglais, impératif)
4. Ne PAS push sauf demande explicite
