# /opennotch:status — État du projet

Résumer l'état actuel :
1. `git status` — fichiers modifiés
2. `git log --oneline -10` — derniers commits
3. `swift build` — compilation OK ?
4. `swift test` — tests OK ?
5. App installée dans /Applications ? Version ?
